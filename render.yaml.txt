services:
  - type: web
    name: MMC-Fortnight-Estimation-Dashboard
    env: python
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    autoDeploy: true